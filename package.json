{
  "name": "gstack-guide",
  "private": true,
  "type": "module",
  "scripts": {
    "render:diagrams": "node tools/render_diagrams.mjs",
    "sanitize:public": "bash tools/public-doc-sanitize-release.sh --dry-run --skip-commit --no-push",
    "sanitize:public:release": "bash tools/public-doc-sanitize-release.sh --allow-empty --omc-note"
  },
  "dependencies": {
    "beautiful-mermaid": "^1.1.3"
  }
}
